export type Pagination = {
  pageNum: number
  pageSize: number
  total: number
}

export type Sorting = {
  sortBy: any
  sortingOrder: any
}

export type Filters = {
  isActive: boolean
  search: string
}
